# LabLock Frontend

React + Vite + TailwindCSS + React Router + Zustand + Axios.

Do **not** create files here by hand — run the Vite generator during Phase 3 kickoff. Instructions are in the root `docs/runbook.md`.

Placeholder so the workspace glob picks up this folder.
